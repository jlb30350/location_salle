<h1>Bienvenue sur notre site de location de salles</h1>
<p>Positionner votre ville afin de la situer.</p>

<div id="search-container">
  <input type="text" id="search-input" placeholder="Entrez un département, une ville ou un code postal">
  <button id="search-button">Rechercher</button>
</div>

<div id="map" style="height: 400px; width: 100%;"></div>
<div id="results-container"></div>

<h2>Vos espaces</h2>

<% if @spaces.any? %>
  <% @spaces.each do |space| %>
    <div class="card">
      <div class="card-header">
        <h3><%= space.title %></h3>
      </div>
      <div class="card-body">
        <p><%= space.description %></p>
        <p><strong>Adresse :</strong> <%= space.address %></p>
        <p><strong>Prix :</strong> <%= number_to_currency(space.price) %></p>
      </div>
      <div class="card-footer">
        <%= link_to 'Voir les détails', space_path(space), class: 'btn btn-primary' %>
        <%= link_to 'Modifier', edit_space_path(space), class: 'btn btn-warning' %>
        <%= link_to 'Supprimer', space_path(space), method: :delete, data: { confirm: 'Êtes-vous sûr de vouloir supprimer cet espace ?' }, class: 'btn btn-danger' %>
      </div>
    </div>
  <% end %>
<% else %>
  <p>Vous n'avez pas encore d'espaces. <%= link_to 'Ajouter un espace', new_space_path %></p>
<% end %>