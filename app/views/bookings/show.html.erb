<h1>Tableau des reservations</h1>

<p>Date de début : <%= @booking.start_date.strftime('%d/%m/%Y') %></p>
<p>Date de fin : <%= @booking.end_date.strftime('%d/%m/%Y') %></p>
<p>Client : <%= @booking.user.email %></p>
<p>Salle réservée : <%= @booking.room.name %></p>
<p>Montant total : <%= number_to_currency(@booking.total_amount) %></p>

<!-- Ajoutez le lien pour télécharger le devis -->
<%= link_to 'Télécharger le devis', invoice_booking_path(@booking, format: :pdf), class: 'btn btn-primary' %>

<!-- Ajoutez un lien pour passer au paiement -->
<%= link_to 'Procéder au paiement', new_room_booking_payment_path(room_id: @room.id, booking_id: @booking.id), class: 'btn btn-primary' %>

