<h1>Tableau des réservations</h1>

<p>Date de début : <%= @booking.start_date.strftime('%d/%m/%Y') %></p>
<p>Date de fin : <%= @booking.end_date.strftime('%d/%m/%Y') %></p>
<p>Client : <%= @booking.user.email %></p>
<p>Salle réservée : <%= @booking.room.name %></p>
<p>Montant total : <%= number_to_currency(@booking.total_amount) %></p>

<!-- Lien pour télécharger le devis -->
<%= link_to 'Télécharger le devis', invoice_booking_path(@booking, format: :pdf), class: 'btn btn-primary' %>

<!-- Lien pour passer au paiement -->
<%= link_to 'Procéder au paiement', new_room_booking_payment_path(room_id: @booking.room.id, booking_id: @booking.id), class: 'btn btn-primary' %>

<!-- Lien pour modifier la réservation -->
<%= link_to 'Modifier la réservation', edit_room_booking_path(@booking.room, @booking), class: 'btn btn-secondary' %>

<!-- Lien pour supprimer la réservation -->
<%= link_to 'Supprimer la réservation', booking_path(@booking), method: :delete, data: { confirm: 'Êtes-vous sûr de vouloir supprimer cette réservation ?' }, class: "btn btn-danger" %>


<!-- Lien pour revenir à la liste des réservations ou à la salle -->
<%= link_to 'Retour à la liste des réservations', room_path(@booking.room), class: 'btn btn-secondary' %>
